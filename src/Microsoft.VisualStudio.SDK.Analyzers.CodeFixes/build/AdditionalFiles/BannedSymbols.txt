P:System.Net.ServicePointManager.SecurityProtocol; Modifying this would affect Visual Studio's process wide networking state.
M:Microsoft.VisualStudio.Shell.Interop.IVsOutputWindowPane.OutputString(System.String); Use OutputStringThreadSafe instead, which is not any more free-threaded but runs a more limited message pump. Even better, consider using IVsOutputWindowPaneNoPump.OutputStringNoPump if the interface is implemented on the target object. Or try OutputWindowTextWriter which is a free-threaded, more modern option.
